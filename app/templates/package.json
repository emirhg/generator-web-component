{
  "devDependencies": {
    "@webdiscus/pug-loader": "^2.10",
    "css-loader": "^6.9.1",
    "pug": "^3.0.2",
    "pug-plugin": "^4.9.9",
    "sass": "^1.70.0",
    "sass-loader": "^14.0.0",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^4.15.1"
  },
  "version": "1.0.0",
  "description": "An awesome web component",
  "name": "web-component",
  "scripts": {
    "build": "webpack --mode=production --node-env=production",
    "build:dev": "webpack --mode=development",
    "build:prod": "webpack --mode=production --node-env=production",
    "prebuild":"rm -f dist/*",
    "watch": "webpack --watch",
    "serve": "webpack serve --mode development --env development --live-reload"
  }
}
